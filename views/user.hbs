<div class="row">
  <img class="img-responsive center-block" src="{{user.image}}" style="width: 250px;margin-bottom: 25px;">
  <div class="col-md-0">
  </div>
  <div class="col-md-6">
    <span>Name</span>
    <h3>{{user.name}}</h3>
    <span>Email</span>
    <h3>{{user.email}}</h3>
    <a href="/users/{{user.id}}/edit"><button class="btn btn-primary my-2 my-sm-0">Edit</button></a>
  </div>